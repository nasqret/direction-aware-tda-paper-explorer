# DenseNet CNN baseline

DenseNet-121 is used as a convolutional neural network baseline trained directly on voxelized structures.

## Role

It is a high-performing voxel model used to evaluate how much predictive signal compact topological descriptors recover.

## Result Pattern

The gap between directional PH+ECP and CNN narrows as anisotropy increases.

